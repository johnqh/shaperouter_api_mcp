# =============================================================================
# ShapeRouter MCP Server Environment Variables
# =============================================================================
# Every value here is optional: the server also reads the same settings from
# ~/.shaperouter/config.json. An environment variable, when non-empty, wins over the
# stored config file.
# =============================================================================

# -----------------------------------------------------------------------------
# API Endpoint (Optional)
# -----------------------------------------------------------------------------
# Base URL of the ShapeRouter API (default: https://api.shaperouter.com)
SHAPEROUTER_API_URL=

# -----------------------------------------------------------------------------
# Credentials (at least one required for anything beyond docs/health)
# -----------------------------------------------------------------------------
# Personal API key — https://shaperouter.com Dashboard -> Settings -> Personal API Keys
SHAPEROUTER_API_KEY=

# Entity (workspace) API key, for workspace-scoped administration
SHAPEROUTER_ENTITY_API_KEY=

# Project API key, for invoking a single project's endpoints
SHAPEROUTER_PROJECT_API_KEY=

# Firebase ID token, as an alternative to an API key
SHAPEROUTER_AUTH_TOKEN=

# -----------------------------------------------------------------------------
# Defaults (Optional)
# -----------------------------------------------------------------------------
# Default entity slug used when a tool call omits one
SHAPEROUTER_ENTITY_SLUG=

# Default organization path used when building AI endpoint URLs
SHAPEROUTER_ORG_PATH=

# -----------------------------------------------------------------------------
# Config File Location (Optional)
# -----------------------------------------------------------------------------
# Override the path of the stored config file
# Default: ~/.shaperouter/config.json
SHAPEROUTER_CONFIG_PATH=
